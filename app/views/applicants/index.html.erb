<p id="notice"><%= notice %></p>
<h1>Users</h1>
<%if current_applicant%>
  <h1><%="It's applicant"%></h1>
<%elsif current_employee%>
  <h1>It's Employee </h1>
<%end%>
<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Email</th>
    <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <% @applicants.each do |user| %>
      <tr>
        <td><%= user.name %></td>
        <td><%= user.email %></td>
      </tr>
    <%end%> 
  </tbody>
</table>
        <%# <td><%= link_to 'Destroy', user, method: :delete,data: { confirm: 'Are you sure?' } %>
      <%= link_to 'Show', applicants_show_path %>
<br></>
      <%= link_to 'Edit', applicants_edit_path%>

<br>
<%= link_to 'New User', new_applicant_session_path %>
